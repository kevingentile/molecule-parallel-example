# molecule-parallel-example

This repository is a placeholder for a potential bug when altering molecule's target host and executing scenario tests with the `--parallel` flag.

```
molecule test --parallel --all
```